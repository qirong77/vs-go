import { ExtensionPopover } from "./ExtensionPopover";

export const ExtensionCookie: React.FC = () => {
  return (
    <ExtensionPopover content={<div>Cookie 管理</div>}>
        🍪
    </ExtensionPopover>
  );
};
